// Phase 1 will fully implement this.
// node-pty MUST only ever be imported in this file (or other files inside electron/).
// Never import node-pty from anywhere under src/.

export class PtyManager {
  // Map of sessionId -> IPty instance (Phase 1).
  // Methods to come: create, write, resize, kill.
}
